# Kopieer naar .env.local en vul je eigen Supabase-waarden in.
# Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only secret (Project Settings → API → service_role). Niet committen.
SUPABASE_SERVICE_ROLE_KEY=
